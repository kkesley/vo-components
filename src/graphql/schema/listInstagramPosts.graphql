
query listInstagramPosts {
    listInstagramPosts {
        items {
            id
            pictureUrl
            likesCount
            commentsCount
            externalUrl
        }
        nextToken
    }
}
